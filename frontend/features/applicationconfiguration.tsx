// import { fi, eo } from "date-fns/locale";
// var setDefaultOptions = require("date-fns/setDefaultOptions");
let appLocale = "";

export const setLocale = (locale: string) => {
  appLocale = locale;
//   import("date-fns/locale/");
//   setDefaultOptions({ locale: new Locale() });
};
